<!DOCTYPE html>
<html lang="en" dir="ltr">
	<meta charset="utf-8">
	<title>Contact</title>
	<h2>Contact me</h2>
		<form action="/contact/send-message" method="POST">
			<label>
				Your name: <input type="text" name="author">
			</label>
			<label>
				Your email: <input type="email" name="sender">
			</label>
			<label>
				Title: <input type="text" name="title">
			</label>
			<label>
				Project design: <input type="file" name="image" accept="image/png, image/jpg, image/jpeg, image/gif">
			</label>
			<div class="isSent">
				{{#if isSent}}
				The message has been sent!
				File {{fileName}} has been saved!
				{{/if}}
			</div>
			<div class="isError">
				{{#if isError}}
				You can't leave fields empty!
				{{/if}}
			</div>
			<label>
				Message: <textarea name="message"></textarea>
			</label>
			<button type="submit">Send message</button>
		</form>
</html>